# TMS — Claude Code

Baca dan ikuti `docs/agent-guide.md` sebagai sumber utama aturan proyek.
Semua path relatif terhadap root repository.

Slash command di `.claude/commands/` meneruskan tugas dan argumen ke
`docs/workflows/`. Definisi `.claude/agents/` merujuk `docs/roles/`.
Gunakan peran secara berurutan; delegasi opsional mengikuti izin dan kemampuan sesi.
Konfigurasi browser MCP Claude ada di `.mcp.json`.
Perbarui prosedur bersama di `docs/`, bukan menyalinnya ke adapter ini.

Mesin ini diduplikasi dari project OMS (`/home/icun/Project/omstest`) pada 2026-09-08.
Pengetahuan spesifik aplikasi OMS (module-map, selector-map, recipe, decisions) sengaja
TIDAK dibawa. Satu-satunya warisan adalah checklist hipotesis di bawah.

## Hipotesis dari OMS — BELUM diverifikasi di TMS

Gotcha teknis yang ditemukan saat mengotomasi OMS. Di TMS semuanya berstatus **hipotesis**:
verifikasi saat `/explore` dan `/harvest-selectors`, **jangan dipakai sebagai aturan otomatis**.
Setelah dicek, ubah kolom Status menjadi `TERVERIFIKASI SAMA` / `BERBEDA` / `BELUM DICEK`
beserta tanggal dan bukti (halaman + selector/DOM yang diperiksa). Bila `BERBEDA`, hapus
sisa asumsi terkait di `tests/helpers/fixtures.js`, `playwright.config.js`, dan `docs/`.

| # | Hipotesis (perilaku di OMS) | Cara verifikasi di TMS (read-only) | Status | Tanggal | Bukti / Catatan |
|---|---|---|---|---|---|
| H1 | `locator.click()` tidak memicu handler React pada sesi Playwright MCP — perlu `locator.dispatchEvent('click')` | Pada 1 halaman list + 1 form: klik tombol yang membuka filter/modal dengan `click()` biasa, cek DOM/network berubah; ulangi dengan `dispatchEvent('click')` | BERBEDA | 2026-09-08 | `/monitoring`: `getByRole('button',{name:'Semua Customer'}).click()` standar langsung mengubah `aria-expanded`→`true` dan memunculkan `[role="listbox"]` + 10 `[role="option"]`. `click()` biasa bekerja normal, `dispatchEvent` tidak diperlukan. Catatan `/shipment`: `click()` bisa timeout bila elemen tepat di bawah header sticky (`z-99999`) yang mengintersep pointer — atasi dengan scroll ke tengah viewport dulu, bukan `dispatchEvent`. Detail: `explore/module-map.md`. |
| H2 | Backend menolak sesi yang dipindah antar browser-context (`storageState` tidak berfungsi) → login 1x per worker, context dibiarkan hidup, `workers: 1` | Login, `context.storageState()`, buka context baru dengan state itu, akses halaman ber-auth — di-redirect ke login atau tidak | BELUM DICEK | — | Sengaja tidak diuji saat `/explore` (butuh multi-context, berisiko session/lock) — `tests/helpers/fixtures.js` & `playwright.config.js` (`workers: 1`) tetap dipertahankan sampai diverifikasi. |
| H3 | Dropdown = custom combobox `button[aria-haspopup="listbox"]` + item `role="option"` (bukan `<select>` native) | Inspeksi 1 dropdown filter (list) dan 1 dropdown form: tag, `aria-haspopup`, role item | BERBEDA (campuran) | 2026-09-08 | Custom combobox terverifikasi di `/monitoring` ("Semua Customer") & filter `/shipment` ("Semua Jenis" dst) — `button[aria-haspopup="listbox"][aria-expanded]` + `[role="option"]`. TAPI filter jenis shipment di `/dashboard-operasional` dan field Status pada modal Edit Provinsi (`/master/provinsi`) adalah `<select>` HTML native + `<option>`. Pola ketiga: kebab "Aksi" per baris (Shipment/Order/Penugasan Tracking/Manajemen Invoice) tanpa ARIA sama sekali (tidak ada `aria-haspopup`, `role="menu"`, `role="menuitem"`) — targetkan item via teks. Pola keempat (harvest shipment 2026-09-08): kartu pilihan jenis pengiriman FTL/FCL/LTL/LCL/Air Freight di form Buat Shipment tanpa ARIA/role — targetkan via teks. Harvest order (2026-09-09): kartu jenis shipment di `/order/buat` pakai `aria-pressed` (beda dari `/shipment/buat`); radio Direct/Connecting = `<input type=radio>` native `sr-only` dengan id stabil (`#fcl-jadwal-direct`, `#fcl-jadwal-connecting`); page-size = `<select>` native. Semua pola hidup berdampingan — cek per komponen saat `/harvest-selectors`. Detail: `explore/module-map.md`. |
| H4 | Datepicker = `div[role="button"]` + grid `button.h-9.w-9`; baris pertama berisi sisa tanggal bulan lalu (tanggal 26–31 ambigu → `.nth(1)`); sebagian instance default bukan bulan berjalan; tidak auto-close pada field tanggal+jam | Buka datepicker di form: struktur sel, header bulan default, isi baris pertama, perilaku close | BERBEDA (campuran) | 2026-09-09 | **Form Buat/Edit Order** (harvest order 2026-09-09): field Tanggal Permintaan Muat / Closing Time / ETD / ETA memakai komponen datetime custom `div[role="button"]` + grid `button.h-9.w-9`, tidak auto-close (pilih tanggal, klik di luar, dan Escape semuanya TIDAK menutup — hanya klik ulang trigger) — struktur OMS **TERVERIFIKASI SAMA untuk instance ini**. Perbedaan: sel bulan lalu/depan TETAP tampil dengan class `text-gray-300` (bukan hidden), jadi angka 26–31 memang ambigu, tetapi pilih lewat `button.h-9.w-9:not(.text-gray-300)` + teks — bukan `.nth(1)`. Bulan default belum dicek. Detail: `shared/selector-map-order.md` "Pola komponen". Sebaliknya filter & dashboard pakai flatpickr: Datepicker "Pilih Tanggal" di `/dashboard-operasional` pakai library flatpickr: sel = `<span class="flatpickr-day">` (bukan `button`) dalam `div.dayContainer`/`div.flatpickr-calendar` (`rangeMode animate multiMonth inline`, 2 bulan sekaligus, inline bukan popup). Default bulan = bulan berjalan (September 2026). Baris pertama berisi sisa tanggal bulan lalu (30-31 Agustus) dengan class `prevMonthDay hidden`. Tidak auto-close (mode rangeMode+inline). Datepicker filter `/shipment` (harvest 2026-09-08): flatpickr **popup** (bukan inline), default bulan berjalan, sisa bulan lalu ber-class `prevMonthDay hidden` (benar-benar tersembunyi → `.nth(1)` tidak relevan), **tidak auto-close** setelah pilih 2 tanggal / klik di luar / Escape — tutup lewat aksi lain (mis. Terapkan). Detail: `explore/module-map.md`, `shared/selector-map-shipment.md`. |
| H5 | Modal/dialog tidak memakai `role="dialog"` | Buka 1 modal lalu `document.querySelector('[role="dialog"]')` | TERVERIFIKASI SAMA | 2026-09-08 | Modal "Edit Provinsi" (`/master/provinsi` → Aksi → Edit Data): overlay `div.fixed.inset-0...z-99999` > `div.relative.rounded-3xl...max-w-lg`, tanpa `role="dialog"` di seluruh rantai parent. `document.querySelector('[role="dialog"]')` = `null` di halaman ini maupun `/monitoring`. Harvest shipment (2026-09-08): 0 `role="dialog"` di ~14 state layar area `/shipment`, termasuk overlay blokir "Fitur belum dapat digunakan"; harvest order (2026-09-09): 0 di seluruh area `/order`. |
| H6 | Tidak ada `data-testid` sama sekali di implementasi | `document.querySelectorAll('[data-testid]').length` pada list + form | TERVERIFIKASI SAMA | 2026-09-08 | `document.querySelectorAll('[data-testid]').length` = 0 di `/monitoring` (list), `/master/provinsi` (list), dan `/shipment/buat` (form); harvest shipment (2026-09-08): 0 di ~14 state layar area `/shipment`; harvest order (2026-09-09): 0 di seluruh area `/order`. |
| H7 | Login: field via placeholder `Masukkan Email` / `Masukkan Password`, tombol `role=button name="Login"`, redirect ke satu halaman dashboard tetap | Kalibrasi login → isi key `login*` di `config/env.md` (format: `config/env.example.md`) | TERVERIFIKASI SAMA | 2026-09-08 | Kalibrasi `/explore`: `[placeholder="Masukkan Email"]`, `[placeholder="Masukkan Password"]`, `role=button[name="Login"]`, redirect selalu ke `/monitoring` (transit sekilas via `/`). Nilai sudah ditulis ke `config/env.md`. |

Catatan tindak lanjut (H1/H3/H4 = BERBEDA): tidak ada implementasi test yang memakai workaround `dispatchEvent('click')`, `.nth(1)` datepicker, atau asumsi pola dropdown tunggal saat ini — `tests/helpers/fixtures.js` dan `playwright.config.js` hanya menyimpan asumsi H2 (`workers: 1`), yang statusnya tetap BELUM DICEK sehingga tidak diubah. `docs/roles/test-executor.md` sudah menggerbangi `dispatchEvent('click')` di belakang status H1 (kini BERBEDA → jangan dipakai kecuali `click()` terbukti tidak bereaksi). Tidak ada perubahan kode yang diperlukan di luar tabel ini.
